const initState = {
    customer_id : "",
    worker_id : "",
    service_id : "",
    services : [],
    status : 'Pending',
    balance : 0.0
}

export const BookingReducer = (state = initState, {type, action}) => {
           
}